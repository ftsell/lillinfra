# bastion networking needs to be configured before everything else so that the other hosts have internet connectivity
- hosts: bastion
  roles:
    - linux_base
    - bastion_router
    - user_accounts

- hosts: all,!bastion
  roles:
    - linux_base
    - user_accounts
