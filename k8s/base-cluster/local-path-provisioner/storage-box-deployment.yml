apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage-box-provisioner
  labels: &labels
    app.kubernetes.io/name: local-path-provisioner
    app.kubernetes.io/instance: storage-box
spec:
  replicas: 1
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:

      serviceAccountName: local-path-provisioner
      volumes:
        - configMap:
            name: storage-box-config
          name: config-volume

      containers:
        - name: local-path-provisioner
          image: rancher/local-path-provisioner
          command:
            - local-path-provisioner
            - --debug
            - start
            - --config
            - /etc/config/config.json
            - --provisioner-name
            - rancher.io/local-path-storage-box
            - --service-account-name
            - local-path-provisioner
            - --configmap-name
            - storage-box-config
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          volumeMounts:
            - mountPath: /etc/config/
              name: config-volume
